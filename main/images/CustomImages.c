#include "CustomImages.h"


// void registerCustomImages() {
//     // This function can be used to register or initialize custom images if needed.
//     // Currently, LVGL handles images via their descriptors, so no action is required here.
//     LV_IMG_DECLARE(turnoutopen);
//     LV_IMG_DECLARE(turnoutclosed);
// }